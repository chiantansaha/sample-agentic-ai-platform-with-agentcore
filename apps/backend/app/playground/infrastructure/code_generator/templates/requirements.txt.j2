# Generated requirements for AgentCore Runtime
# Agent: {{ agent_name }}
# Version: {{ agent_version }}

{% if not base_image_uri %}
# Common dependencies (included in base image if base_image_uri is set)
# AgentCore Runtime SDK (provides BedrockAgentCoreApp with /invocations and /ping endpoints)
bedrock-agentcore

# Strands Agents SDK
strands-agents>=0.1.0

# AWS SDK
boto3>=1.34.0

# AWS Distro for OpenTelemetry (required for Strands in AgentCore Runtime)
# See: https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/observability-configure.html
aws-opentelemetry-distro>=0.10.0

# Utilities
python-json-logger>=2.0.0

# MCP (Model Context Protocol) SDK (commonly used for Gateway integration)
mcp>=1.0.0

# AWS IAM authenticated MCP client (required for Gateway with IAM authentication)
mcp-proxy-for-aws>=0.1.0
{% endif %}

{% if has_tools %}
# Strands Community Tools (file_read, file_write, editor, etc.)
# Note: Package name is strands-agents-tools (NOT strands-tools)
strands-agents-tools>=0.1.0
{% endif %}

{% if has_session_manager %}
# Session Management
# S3SessionManager is included in strands-agents
{% endif %}

{% if knowledge_bases %}
# Knowledge Base Tools
# boto3 bedrock-agent-runtime is used for KB retrieval
{% endif %}
